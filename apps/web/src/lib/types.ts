/**
 * @deprecated Import directly from `@docmosaic/core`. This shim exists
 * for back-compat during the refactor and will be removed in Phase 10.
 */
export type {
    DragPosition,
    Document,
    ImageSection,
    MeasurementUnit,
    Page,
    PageDimensions,
    PageOrientation,
    PageSize,
    PDFDocument,
    PDFGenerationOptions,
    ResizeInfo,
    Section,
} from '@docmosaic/core';
