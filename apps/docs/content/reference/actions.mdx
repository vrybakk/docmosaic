---
title: Actions
description: The EditorActions surface — every dispatcher exposed by useEditor() and useDocumentState().
---

`actions` is a flat, referentially stable object of dispatchers. Same surface whether you reach it through `useEditor().actions` (compound primitives) or `useDocumentState().actions` (BYO-UI).

## Sections

| Method                     | Effect                                                                                                                                                        |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `addSection(input)`        | Insert a new section. `input` carries `{ pageIndex, type, ... }`. `type` accepts `'frame'`; an image may pass `maskShape` for a placeholder/image-mask frame. |
| `updateSection(id, patch)` | Merge `patch` into the section identified by `id`.                                                                                                            |
| `deleteSection(id)`        | Remove the section. Deleting a container frame also removes its children (`parentFrameId`). Clears selection if it was selected.                              |
| `duplicateSection(id)`     | Clone the section onto the same page with a small offset. Duplicating a container frame also clones its children, re-pointed at the new frame.                |
| `setHidden(id, hidden)`    | Explicitly set the hidden flag.                                                                                                                               |
| `toggleHidden(id)`         | Flip the hidden flag.                                                                                                                                         |
| `setLocked(id, locked)`    | Explicitly set the locked flag.                                                                                                                               |
| `toggleLocked(id)`         | Flip the locked flag.                                                                                                                                         |

## Layers

| Method             | Effect                                                |
| ------------------ | ----------------------------------------------------- |
| `bringToFront(id)` | `zIndex = max(peers) + 1`.                            |
| `sendToBack(id)`   | `zIndex = min(peers) - 1`.                            |
| `moveForward(id)`  | Swap `zIndex` with next-higher peer on the same page. |
| `moveBackward(id)` | Swap `zIndex` with next-lower peer on the same page.  |

## Pages

| Method                                     | Effect                                                         |
| ------------------------------------------ | -------------------------------------------------------------- |
| `addPage()`                                | Append a new empty page.                                       |
| `deletePage(pageIndex)`                    | Remove the page and every section on it.                       |
| `reorderPages(from, to)`                   | Move a page within the list; rewrites `pageIndex` on sections. |
| `setPageBackground(pageIndex, background)` | Update the page's background color or image.                   |
| `addGuide(pageIndex, axis, position)`      | Add a vertical / horizontal guide line on the page.            |
| `removeGuide(pageIndex, axis, position)`   | Remove the matching guide.                                     |

## Document properties

| Method                        | Effect                            |
| ----------------------------- | --------------------------------- |
| `setDocumentName(name)`       | Update `Document.name`.           |
| `setPageSize(size)`           | Switch the document's `pageSize`. |
| `setOrientation(orientation)` | Switch portrait / landscape.      |

## History

| Method   | Effect                          |
| -------- | ------------------------------- |
| `undo()` | Step backwards on the timeline. |
| `redo()` | Step forwards.                  |

## UI

| Method                             | Effect                                  |
| ---------------------------------- | --------------------------------------- |
| `selectSection(id \| null)`        | Set / clear the current selection.      |
| `setCurrentPage(pageIndex)`        | Switch the active page in the canvas.   |
| `openPreview()` / `closePreview()` | Toggle the preview dialog.              |
| `cancelGeneration()`               | Abort the in-flight PDF/PNG generation. |

## Stability

`actions` is referentially equal across renders. Safe to put in dependency arrays without triggering re-runs.

## See also

-   [Hooks reference](/docs/reference/hooks)
-   [Types reference](/docs/reference/types)
